{
  "name": "flick",
  "scripts": {
    "compile": "bun build --compile --minify index.ts --outfile build/flick"
  },
  "devDependencies": {
    "@types/bun": "^1.1.1",
    "bun-types": "^1.1.7",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "net": "^1.0.2",
    "zod": "^3.23.7"
  }
}
